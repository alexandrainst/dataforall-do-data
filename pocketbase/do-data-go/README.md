Pocketbase Go project

* To init the dependencies, run `go mod init do-data-go && go mod tidy.`
* To start the application, run `go run . serve.`
* To build a statically linked executable, run `go build` and then you can start the created executable with `./do-data-go serve`.